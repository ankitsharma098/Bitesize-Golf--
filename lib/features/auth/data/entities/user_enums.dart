enum AccountStatus { active, suspended, deleted }

// Enums for subscription
enum SubscriptionStatus { active, cancelled, expired, free }

enum SubscriptionTier { free, premium, trial }

enum UserRole { pupil, coach, guest, admin }
